encapsulation: the packing of data members(primitive data, methods, etc.) into
        an object and restricting outside access to them. ex.) facebook info

class Classname:
    everything indented under the class is contained in the class

constructors are similar to methods: but they only have one pre-defined purpose
    which is always called "__init__" followed by parameters

    compile error if you do no have two underscored on either side of init

instance variables =  self prefix, self referring to the object being created,
        which the instance variables belong to
We use empty Strings for String values, and 0 for number values.
