class MilesPerHour:
    #Constructor
    def ___init___(self, distance, hours, minutes)
        self.calcDistance = distance
        self.calcHours = hours
        self.calcMinutes = minutes
        mph = 0

    #Modifier
    def SetValues(self, distance, hours, minutes)
        self.calcDistance = distance
        self.calcHours = hours
        self.calcMinutes = minutes
        mph = 0
